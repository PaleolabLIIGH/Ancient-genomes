Written by Emanuel Martínez

If your dataset includes modern or ancient fastq use Method 1. 
If your dataset are modern fasta use Method 2.

## Method 1
If the sequences you want to download are from NCBI, the easiest way to do this is by using the SRA Toolkit. 
### 13.1 Get the run accessions
Create a txt file with only that data:
```
nano ID_SRR.list
head ID_SRR.list
```
```
#Output:
SRR346694
SRR513801
SRR514329
SRR062298
SRR062302
SRR062339
SRR062477
SRR062501
SRR1805092
SRR512085
```
### 13.2 Get the sequences in the cluster
Copy ID_SRR.list to the cluster into your working folder
```
cd /panfs/pan5/mavila/lcarrillo/modern_comparation/USA_Dental_plaque
ls
```
```
#Output
ID_SRR.list
```
Copy the following script to your folder
```
cp /mnt/data/mavila/lcarrillo/scripts/faster_for.sh .
```
This is the script:
```
for i in $(cat ID_SRR.list)
do
	prefetch $i -o $i.sra
	fastq-dump --skip-technical -I -W --split-files --gzip $i.sra
	rm -r $i.sra
done
```
Create an screen and activate the necessary modules
```
screen -S dental_usa
module load sra/3.0.0
bash faster_for.sh
```
```
#Output:
2024-11-09T17:32:41 prefetch.3.0.0: Current preference is set to retrieve SRA Lite files with simplified base quality scores.
2024-11-09T17:32:41 prefetch.3.0.0: 1) Downloading 'SRR346694'...
2024-11-09T17:32:41 prefetch.3.0.0: SRA Normalized Format file is being retrieved, if this is different from your preference, it may be due to current file availability.
2024-11-09T17:32:41 prefetch.3.0.0:  Downloading via HTTPS...
```
Depending on the number of sequences you're downloading, this may take a while. Therefore, it's better to close the screen session with `Ctrl + 
A + D` to avoid interrupting the download.
Transform your `.gz` files to `.bz2` 

# After Method 1 
### 13.3 Remove Adapters
If you used the Method1 use this script because the format in those fastq is different of the ones we get from our samples:

For paired-end sequences
```
cp /mnt/data/mavila/lcarrillo/scripts/Adapter_V2_pair_end_lco_corrected_modern_samples_different_mate_separator_parallell.sh /mnt/data/mavila/lcarrillo/scripts  # Modify the path to your scripts folder
```
Run it. Once you have the fastq ready map them to the human reference and use the unmapped fraction to map it with the reference genome of interest.

## Method 2
Create a list of the GCF IDs that you need
```
nano assemblies.txt
head assemblies.txt
```
```
#Output:
GCF_904859905.1
GCF_036509175.1
GCF_036509015.1
GCF_020575245.1
GCF_003665595.1
GCF_013389635.1
GCF_033164915.1
GCF_030177735.1
GCF_014725915.1
GCF_052952515.1
```
Copy the next script:
```
/mnt/data/mavila/lcarrillo/scripts/get-genomes.sh
```
Modify the genus of interest:
```
#!/bin/bash
# Written by Laura Carrillo Olivas

# Download GCF assemblies from NCBI RefSeq
# and save genomic FASTA files into genomes/

set -euo pipefail

# Input
ACCESSIONS_FILE="assemblies.txt"
SUMMARY_REFSEQ="assembly_summary_refseq.txt"
GENUS="Citrobacter" #Modified if needed

# Output
OUTDIR="genomes"

mkdir -p "$OUTDIR"

# Download RefSeq assembly summary

if [ ! -f "$SUMMARY_REFSEQ" ]; then
    echo "Downloading RefSeq assembly_summary..."
    wget -q ftp://ftp.ncbi.nlm.nih.gov/genomes/refseq/bacteria/assembly_summary.txt \
        -O "$SUMMARY_REFSEQ"
fi

# Filter Genus + selected GCF accessions

grep -Ff "$ACCESSIONS_FILE" "$SUMMARY_REFSEQ" \
    | grep -i "$GENUS" \
    > filtered_assemblies.txt

# Download genomic FASTA only

while read -r line; do
    FTP_URL=$(echo "$line" | cut -f20)   # ftp_path

    if [[ "$FTP_URL" == ftp://* || "$FTP_URL" == https://* ]]; then
        BASE=$(basename "$FTP_URL")
        FILE="$OUTDIR/${BASE}_genomic.fna.gz"

        echo "Downloading $BASE ..."
        wget -q -O "$FILE" "${FTP_URL}/${BASE}_genomic.fna.gz"

        if [ ! -s "$FILE" ]; then
            echo "WARNING: failed to download $BASE"
            rm -f "$FILE"
        fi
    fi
done < filtered_assemblies.txt
```


If you use the method 2 then go to https://github.com/PaleolabLIIGH/Ancient-genomes/blob/main/14.Mutivcf_analysis.md
