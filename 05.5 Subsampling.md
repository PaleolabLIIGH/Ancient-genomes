### 5.5.1 Study the program
Normalization is recommended if the series datasets are different sizes—log transformation. Alternatively, within-sample proportional normalization can be implemented in R or even excel once the data 
table has been generated. 
You can use a subsampling since the raw data (after adapter removal) or if you are interested only in the ummapped reads with the unmmaped fastq.

You can learn more about subsampling here:
```
https://github.com/lh3/seqtk
https://link.springer.com/protocol/10.1007/978-1-4939-8728-3_14
```

### 5.5.2 If you want to do it since you have your original unmmaped files
5.5.2.1 Check how many reads you have in the unmapped sequences
```
qlogin -pe openmp 12
cd /panfs/pan5/mavila/lcarrillo/Conchita-concatenado/Mapping_hg38/Unmapped/
for file in *.fastq.collapsed.unmapped.fastq.bz2
do
    if [ -e "$file" ]; then  # Verifica que el archivo existe
        count=$(bzcat "$file" | wc -l)
        reads=$((count / 4))
        echo "$file: $reads reads"
    else
        echo "No hay archivos que coincidan con el patrón."
    fi
done
``` 
You'll see in the terminal something similar to:
``` 
C4-concatenado_L001_R1-2_001.fastq.collapsed.unmapped.fastq.bz2: 46558021 reads
E10-concatenado_L001_R1-2_001.fastq.collapsed.unmapped.fastq.bz2: 54785334 reads
``` 

### 5.5.3 If you desire to do a subsampling after adapter removal
Go to your directory where you have your collapsed sequences:
```
cd /mnt/Cromosoma/mavila/lcarrillo/HSJN-concatenado/
ls
```
```
#Output:
001-H089_adapter_collapsed.txt  057-H334_adapter_collapsed.txt
001-H089_fastq                  057-H334_fastq
001-H089_L001_R1_001.fastq.bz2  057-H334_L001_R1_001.fastq.bz2
001-H089_L001_R2_001.fastq.bz2  057-H334_L001_R2_001.fastq.bz2
002-H090_adapter_collapsed.txt  058-H335_adapter_collapsed.txt
002-H090_fastq                  058-H335_fastq
002-H090_L001_R1_001.fastq.bz2  058-H335_L001_R1_001.fastq.bz2
002-H090_L001_R2_001.fastq.bz2  058-H335_L001_R2_001.fastq.bz2
003-H095_adapter_collapsed.txt  061-H336_adapter_collapsed.txt
Mapping_hg38                    AdapterremovalLCO.o367938
AdapterremovalLCO.po367938      Adapter_V2_pair_end_lco_corrected.sh
get-table-adapter.sh
```

Create a new folder for your subsampled sequences and symbolic links for the sequences
```
mkdir subsampling
for file in $(find /mnt/Cromosoma/mavila/lcarrillo/HSJN-concatenado/ -type f -name "*collapsed.bz2"); do
  cp $file subsampling
done
cd subsampling
```

You already have a table with the information of how many reads you got in each of your sequences. In this case the sequence with less information had 10354090 reads after removing adapters, so I'll
normalize the rest of them with that size (except negatives, that have much less information) with the next code:
```
#Step1: Check that the future names are correct:
for file in $(find /mnt/Cromosoma/mavila/lcarrillo/HSJN-concatenado/subsampling -type f -name "*bz2*"); do
  name=$(basename $file | cut -d "_" -f 1)
  echo $name
  new_name=$(echo "$name_L001_R1-2_001.sub.fastq.collapsed")
  echo $new_name
done
#Step2: Subsampling process:
#If the name are correct then use the next one:
for file in $(find /mnt/Cromosoma/mavila/lcarrillo/HSJN-concatenado/subsampling -type f -name "*bz2*"); do
  name=$(basename $file | cut -d "_" -f 1)
  echo $name
  new_name=$(echo "$name_L001_R1-2_001.sub.fastq.collapsed")
  /mnt/Cromosoma/mavila/vvilla/scripts/seqtk/seqtk sample -s100 <(bzcat $file) 10354090 > $new_name #Remember to chance the size, in this case I used 10354090
done
```
Now you have the original and the subsampled sequences in this folder, but you already have the originals in their respective fastq folder, so it is not necessary to keep those here, and you can 
remove them as follows:
```
rm *_L001_R1-2_001.fastq.collapsed.bz2 #Modify if necessary
ls
```
```
#Output:
001-H089_L001_R1-2_001.sub.fastq.collapsed.bz2
002-H090_L001_R1-2_001.sub.fastq.collapsed.bz2
003-H095_L001_R1-2_001.sub.fastq.collapsed.bz2
004-H139_L001_R1-2_001.sub.fastq.collapsed.bz2
005-H152_L001_R1-2_001.sub.fastq.collapsed.bz2
006-H153_L001_R1-2_001.sub.fastq.collapsed.bz2
007-H157_L001_R1-2_001.sub.fastq.collapsed.bz2
009-H160_L001_R1-2_001.sub.fastq.collapsed.bz2
010-H181_L001_R1-2_001.sub.fastq.collapsed.bz2
013-H184_L001_R1-2_001.sub.fastq.collapsed.bz2
```
Do the mapping and get the unmapped sequences, to do the kraken analysis. 

Once you have the new reports prepare the files that you need to do abundance analysis in https://github.com/PaleolabLIIGH/Ancient-genomes/blob/main/06.Preparation_to_abundance_analysis.md
