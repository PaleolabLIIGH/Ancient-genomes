### 11.1 Study about preseq
You can learn more here: https://github.com/smithlabcode/preseq/tree/master

### 11.2 Work with preseq
Go to your work directory and load the modules

```
qlogin
screen
cd /mnt/Cromosoma/mavila/lcarrillo/capturas_salmonella_myco/Mapping_hg38/Unmapped/Salmonella_enterica_Pc
module load gcc/5.1.0
module load preseq/1.0.2
module load bedtools/2.26
```

Perform a loop through all files ending with `*sort.bam`:

```
### Written by Rigoberto Padilla
### Modified by Laura Carrillo Olivas to run it in screen instead of a job
for file in *sort.bam; do
  #Check if the file exists:
  if [ -f "$file" ]; then
    #Extract the file prefix (filename without the .bam extension):
    prefix=$(basename "$file" .bam)
    # Execute preseq lc_extrap and save the output
    preseq lc_extrap -B -o "${prefix}_yield_estimates.txt" "$file"
    # Execute preseq c_curve and save the output
    preseq c_curve -B -o "${prefix}_complexity_output.txt" "$file"
    # Execute bam2mr and save the output
    bam2mr "$file" -o "${prefix}.mr" -L300 -R 25000000
    # Sort the output of bam2mr
    sort -k 1,1 -k2,2n -k3,3n "${prefix}.mr" > "${prefix}.mr.sort"
    # Execute preseq gc_extrap and save the output
    preseq gc_extrap "${prefix}.mr.sort" -o "${prefix}_future_coverage.txt"
    #Print a message indicating the file has been processed:
    echo "Processed: $file"
  fi
done
Ctrl + a d
#Check the progress time to time with `screen -r`.
#Once the work had finished `exit`.
```

```
#Output:
Processed: 021-H238-WISC_L001_R1-2_001.fastq.collapsed.unmapped.fastq.bwa.sort.bam
ERROR:	too many iterations, poor sample
ERROR:	too many iterations, poor sample
Processed: 041-H302-WISC_L001_R1-2_001.fastq.collapsed.unmapped.fastq.bwa.sort.bam
ERROR:	Library expected to saturate in doubling of size, unable to extrapolate
ERROR:	Library expected to saturate in doubling of experiment size, unable to extrapolate
Processed: 091-H337bis-WISC_L001_R1-2_001.fastq.collapsed.unmapped.fastq.bwa.sort.bam
```

```
ls -lthr
```

### 11.3 Work in RStudio

Download the outups


